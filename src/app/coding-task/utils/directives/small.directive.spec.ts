import { SmallDirective } from './small.directive';

describe('SmallDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallDirective();
    expect(directive).toBeTruthy();
  });
});
